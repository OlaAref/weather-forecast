spring:
  application:
    name: weather
  datasource:
    url: jdbc:mysql://localhost:3306/weather_db
    username: weatherApp
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
